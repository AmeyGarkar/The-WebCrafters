<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wearify - Wearables & Audio</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 text-gray-800">

    <!-- Header (include partial) -->
    <%- include("partials/header") %>

    <!-- Banner -->
    <section class="bg-blue-100 text-center py-12 mb-10">
      <h1 class="text-4xl font-bold mb-2">Your Tech Companion</h1>
      <p class="text-gray-600">Wearables, Earbuds, Smart Audio, and More</p>
    </section>

    <!-- HERO SLIDER -->
    <section class="relative w-full h-[80vh] bg-blue-100 rounded-b-2xl overflow-hidden">
    <div class="flex transition-transform duration-500 ease-in-out h-full slides">
      <!-- Slide 1 -->
      <div class="min-w-full flex justify-between items-center px-10">
        <div class="max-w-md">
          <h4 class="text-sm text-gray-600 mb-2">Air Clips 2</h4>
          <h1 class="text-4xl font-bold text-black leading-tight">Live in the<br>moment</h1>
          <p class="text-gray-700 my-4">Special launch offer</p>
          <button class="bg-black text-white px-6 py-2 rounded-full font-semibold">Shop Now</button>
        </div>
        <img src="images/slide1.jpg" alt="Air Clips - Wireless Earbuds" class="w-[400px] h-auto rounded-xl" />
      </div>

      <!-- Slide 2 -->
      <div class="min-w-full flex justify-between items-center px-10">
        <div class="max-w-md">
          <h4 class="text-sm text-gray-600 mb-2">Wave Ultra</h4>
          <h1 class="text-4xl font-bold text-black leading-tight">Style meets<br>function</h1>
          <p class="text-gray-700 my-4">Smartwatch Series</p>
          <button class="bg-black text-white px-6 py-2 rounded-full font-semibold">Explore</button>
        </div>
        <img src="images/slide2.jpg" alt="Wave Ultra Smartwatch" class="w-[400px] h-auto rounded-xl" />
      </div>

      <!-- Slide 3 -->
      <div class="min-w-full flex justify-between items-center px-10">
        <div class="max-w-md">
          <h4 class="text-sm text-gray-600 mb-2">Portable Beats</h4>
          <h1 class="text-4xl font-bold text-black leading-tight">Feel the<br>power</h1>
          <p class="text-gray-700 my-4">Mini Bluetooth Speakers</p>
          <button class="bg-black text-white px-6 py-2 rounded-full font-semibold">Buy Now</button>
        </div>
        <img src="images/slide3.jpg" alt="Portable Bluetooth Speakers" class="w-[400px] h-auto rounded-xl" />
      </div>
    </div>

    <!-- Indicators -->
    <div class="absolute bottom-4 left-1/2 transform -translate-x-1/2 flex gap-3 indicators">
      <span class="dot w-3 h-3 rounded-full bg-black cursor-pointer" data-index="0"></span>
      <span class="dot w-3 h-3 rounded-full bg-gray-400 cursor-pointer" data-index="1"></span>
      <span class="dot w-3 h-3 rounded-full bg-gray-400 cursor-pointer" data-index="2"></span>
    </div>
  </section>








    <!-- Top Picks Section -->
    <main class="container mx-auto px-4">
      <h2 class="text-2xl font-semibold mb-6">Top Picks For You</h2>

      <% if (products && products.length > 0) { %>
        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols gap-6">
          <% products.forEach(product => { %>
            <a href="/product/<%= product.slug %>" class="group">
              <div class="bg-white p-4 rounded-xl shadow hover:shadow-lg transition-all">
                <img src="<%= product.image %>" alt="<%= product.name %>"
                     class="w-full h-40 object-cover rounded mb-2" />

                <h3 class="font-semibold text-lg text-gray-800 group-hover:text-blue-600">
                  <%= product.name %>
                </h3>

                <div class="text-sm text-gray-500 mb-1">
                  <% if (product.features && product.features.length > 0) { %>
                    <%= product.features[0] %>
                  <% } %>
                </div>

                <div class="flex items-center gap-2">
                  <span class="text-lg font-bold text-green-600">₹<%= product.price %></span>
                  <% if (product.originalPrice && product.originalPrice > product.price) { %>
                    <span class="text-sm line-through text-gray-400">₹<%= product.originalPrice %></span>
                  <% } %>
                </div>
              </div>
            </a>
          <% }); %>
        </div>
      <% } else { %>
        <p class="text-gray-500">No products found.</p>
      <% } %>
    </main>

     <main class="container mx-auto px-4">
      <h2 class="text-2xl font-semibold mb-6">Top Picks For You</h2>

      <% if (products && products.length > 0) { %>
        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols gap-6">
          <% products.forEach(product => { %>
            <a href="/product/<%= product.slug %>" class="group">
              <div class="bg-white p-4 rounded-xl shadow hover:shadow-lg transition-all">
                <img src="<%= product.image %>" alt="<%= product.name %>"
                     class="w-full h-40 object-cover rounded mb-2" />

                <h3 class="font-semibold text-lg text-gray-800 group-hover:text-blue-600">
                  <%= product.name %>
                </h3>

                <div class="text-sm text-gray-500 mb-1">
                  <% if (product.features && product.features.length > 0) { %>
                    <%= product.features[0] %>
                  <% } %>
                </div>

                <div class="flex items-center gap-2">
                  <span class="text-lg font-bold text-green-600">₹<%= product.price %></span>
                  <% if (product.originalPrice && product.originalPrice > product.price) { %>
                    <span class="text-sm line-through text-gray-400">₹<%= product.originalPrice %></span>
                  <% } %>
                </div>
              </div>
            </a>
          <% }); %>
        </div>
      <% } else { %>
        <p class="text-gray-500">No products found.</p>
      <% } %>
  

     
      <h2 class="text-2xl font-semibold mb-6">Top Picks For You</h2>

      <% if (products && products.length > 0) { %>
        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols gap-6">
          <% products.forEach(product => { %>
            <a href="/product/<%= product.slug %>" class="group">
              <div class="bg-white p-4 rounded-xl shadow hover:shadow-lg transition-all">
                <img src="<%= product.image %>" alt="<%= product.name %>"
                     class="w-full h-40 object-cover rounded mb-2" />

                <h3 class="font-semibold text-lg text-gray-800 group-hover:text-blue-600">
                  <%= product.name %>
                </h3>

                <div class="text-sm text-gray-500 mb-1">
                  <% if (product.features && product.features.length > 0) { %>
                    <%= product.features[0] %>
                  <% } %>
                </div>

                <div class="flex items-center gap-2">
                  <span class="text-lg font-bold text-green-600">₹<%= product.price %></span>
                  <% if (product.originalPrice && product.originalPrice > product.price) { %>
                    <span class="text-sm line-through text-gray-400">₹<%= product.originalPrice %></span>
                  <% } %>
                </div>
              </div>
            </a>
          <% }); %>
        </div>
      <% } else { %>
        <p class="text-gray-500">No products found.</p>
      <% } %>
    </main>



    <!-- Footer -->
    <footer class="bg-white mt-16 py-6 text-center text-sm text-gray-400 border-t">
      &copy; <%= new Date().getFullYear() %> Wearify. All rights reserved.
    </footer>



    <script>
  document.addEventListener('DOMContentLoaded', () => {
    // ====================
    // Slider Logic
    // ====================
    let currentSlide = 0;
    const slides = document.querySelector('.slides');
    const dots = document.querySelectorAll('.dot');

    function goToSlide(index) {
      currentSlide = index;
      updateSlider();
    }

    function updateSlider() {
      slides.style.transform = `translateX(-${currentSlide * 100}%)`;
      dots.forEach((dot, idx) => {
        dot.classList.remove('bg-black', 'bg-gray-400');
        dot.classList.add(idx === currentSlide ? 'bg-black' : 'bg-gray-400');
      });
    }

    function startAutoSlide() {
      setInterval(() => {
        currentSlide = (currentSlide + 1) % dots.length;
        updateSlider();
      }, 5000);
    }

    dots.forEach(dot => {
      dot.addEventListener('click', () => {
        const index = parseInt(dot.getAttribute('data-index'), 10);
        goToSlide(index);
      });
    });

    updateSlider();
    startAutoSlide();

  });

</script>


  </body>
</html>
